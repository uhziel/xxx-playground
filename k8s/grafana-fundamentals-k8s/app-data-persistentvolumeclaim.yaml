apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app_data
spec:
  storageClassName: manual
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
